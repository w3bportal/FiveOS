<ui:FluentWindow x:Class="FiveOS.Views.XmlEditorWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Edit XML"
                 Height="620" Width="820"
                 MinHeight="360" MinWidth="480"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="None"
                 ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="Edit XML"
                     ShowMaximize="True"
                     ShowMinimize="False" />

        <StackPanel Grid.Row="1" Margin="16,4,16,8">
            <TextBlock x:Name="PathText" FontSize="11" FontFamily="Consolas" Opacity="0.7"
                       TextTrimming="CharacterEllipsis" />
        </StackPanel>

        <Border Grid.Row="2" Margin="16,0,16,0"
                CornerRadius="6"
                BorderThickness="1"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                ClipToBounds="True">
            <TextBox x:Name="Editor"
                     BorderThickness="0"
                     Background="Transparent"
                     FontFamily="Consolas" FontSize="12"
                     Padding="10,8"
                     AcceptsReturn="True" AcceptsTab="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextChanged="OnTextChanged" />
        </Border>

        <Grid Grid.Row="3" Margin="16,12,16,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="StatusText"
                       FontSize="11" Opacity="0.7" VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis" />

            <ui:Button Grid.Column="1" Content="Reload"
                       Appearance="Secondary" Padding="16,6" Margin="0,0,8,0"
                       Click="OnReload"
                       ToolTip="Discard edits and reload the file from disk." />
            <ui:Button Grid.Column="2" Content="Close"
                       Appearance="Secondary" Padding="16,6" Margin="0,0,8,0"
                       Click="OnClose" />
            <ui:Button Grid.Column="3" x:Name="SaveButton" Content="Save"
                       Appearance="Primary" Padding="20,6" IsEnabled="False"
                       Click="OnSave"
                       ToolTip="Validate as XML and write back to the file in place." />
        </Grid>
    </Grid>
</ui:FluentWindow>
