<ui:FluentWindow x:Class="FiveOS.Views.SimsClipPickerWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Import Sims clip"
                 Width="480" Height="420"
                 MinWidth="400" MinHeight="320"
                 ResizeMode="CanResize"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Import Sims clip"
                     ShowMaximize="False" ShowMinimize="False" />

        <TextBlock Grid.Row="1"
                   Margin="20,8,20,8"
                   TextWrapping="Wrap"
                   FontSize="13"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Text="This package contains multiple animations. Pick one to load onto the Emotes timeline." />

        <ListBox x:Name="ClipList"
                 Grid.Row="2"
                 Margin="20,0,20,8"
                 DisplayMemberPath="Display"
                 MouseDoubleClick="OnListDoubleClick" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,8,16,16">
            <ui:Button Content="Cancel" Appearance="Secondary" Padding="18,6"
                       Margin="0,0,8,0" Click="OnCancel" />
            <ui:Button Content="Import" Appearance="Primary" Padding="20,6"
                       Click="OnImport" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
