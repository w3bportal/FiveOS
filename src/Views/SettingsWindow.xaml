<ui:FluentWindow x:Class="FiveOS.Views.SettingsWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 mc:Ignorable="d"
                 Title="Settings"
                 Width="880" Height="620"
                 MinWidth="700" MinHeight="480"
                 ResizeMode="CanResize"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="None"
                 ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="Settings"
                     ShowMaximize="False"
                     ShowMinimize="False" />

        <!-- The existing SettingsView (nav + pages) is injected here in
             code-behind so it keeps its lazy on-load scans off the app boot. -->
        <ContentControl Grid.Row="1" x:Name="Host" />
    </Grid>
</ui:FluentWindow>
