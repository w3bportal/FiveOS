<ui:FluentWindow x:Class="FiveOS.Views.AiGeneratorWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:views="clr-namespace:FiveOS.Views"
                 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 mc:Ignorable="d"
                 Title="AI generator"
                 Height="720" Width="1080"
                 MinHeight="560" MinWidth="900"
                 WindowBackdropType="None"
                 ExtendsContentIntoTitleBar="True"
                 WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="AI generator"
                     ShowMaximize="True"
                     ShowMinimize="False"
                     ShowClose="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/Branding/FiveOS.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="452" />
                <ColumnDefinition Width="*" MinWidth="340" />
            </Grid.ColumnDefinitions>

            <views:ImageTo3DView Grid.Column="0"
                                 x:Name="GeneratorView" />

            <Border Grid.Column="1"
                    BorderThickness="1,0,0,0"
                    BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                    Background="{DynamicResource ApplicationBackgroundBrush}">
                <Grid>

                    <StackPanel x:Name="PreviewEmpty"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <ui:SymbolIcon Symbol="Cube24" FontSize="40" Opacity="0.35"
                                       HorizontalAlignment="Center" Margin="0,0,0,10" />
                        <TextBlock Text="Your generated model appears here"
                                   Opacity="0.55" FontSize="13" HorizontalAlignment="Center" />
                        <TextBlock Text="drag to rotate · scroll to zoom"
                                   Opacity="0.4" FontSize="11" HorizontalAlignment="Center" Margin="0,2,0,0" />
                    </StackPanel>

                    <wv2:WebView2 x:Name="PreviewViewport" Visibility="Collapsed" />
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Padding="14,10"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           x:Name="StatusText"
                           VerticalAlignment="Center"
                           FontSize="11"
                           Opacity="0.75"
                           Text="Generate a model, then click Use to load it into the 3D Model viewer." />
                <ui:Button Grid.Column="1"
                           Content="Cancel"
                           Appearance="Secondary"
                           Padding="14,6"
                           Margin="0,0,8,0"
                           Click="OnCancel"
                           ToolTip="Close the generator without loading anything. Already-generated GLBs stay on disk in ~/Downloads/FiveOS_meshes/generated/." />
                <ui:Button Grid.Column="2"
                           x:Name="UseButton"
                           Content="Use latest"
                           Appearance="Primary"
                           Padding="18,6"
                           IsEnabled="False"
                           Click="OnUseLatest"
                           ToolTip="Load the most-recently-generated GLB into the 3D Model viewer. Enabled after at least one successful generation in this session.">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="ArrowRight24" />
                    </ui:Button.Icon>
                </ui:Button>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
